# .env.example — secrets and per-environment connection strings only.
# All structured feature config lives in config.yaml.

# Telegram
TELEGRAM_BOT_TOKEN=

# LLM API keys (structured config in config.yaml)
OPENROUTER_API_KEY=
LOCAL_API_KEY=ollama
CUSTOM_API_KEY=

# PostgreSQL credentials
POSTGRES_USER=sebson
POSTGRES_PASSWORD=
POSTGRES_DB=sebsonbot
DATABASE_URL=postgresql://sebson:password@localhost:5432/sebsonbot
POSTGRES_PORT=5432
